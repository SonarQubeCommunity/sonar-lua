local DoTheJob = function(ctx)--1
 
local modName = "SqlServerToVtype"
 
            exec = exec or 0
end
--
function add (a,b)--2
       print("foo", a)--3
       return coroutine.yield(2*a)--4
     end
--   

     co = coroutine.create(function (a,b)--5,6
      
           return b, "end"     
     end)
                  
 Print(test)--7
 
 
 --if statements   
    if i > 20 then
    function add (a,b)--8
       print("foo", a)--9
       return coroutine.yield(2*a)--10
     end
      local x          -- local to the "then" body
      x = 20
      print(x + 2)--11
    else
      print(x)         --> 12 (the global one)
    end