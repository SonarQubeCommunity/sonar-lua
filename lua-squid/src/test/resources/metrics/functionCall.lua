

-- | Construct a multi-level bidirectional network.
--rnnlib.makeBidirectional = a
fi = 0
    if i < 5 then
      print("hello")--one
    else
      -- print("goodbye")
    end
    
    local function getHello(who)
  return "hello "..who
end

print(getHello("Chris"))--2 and 3 
    
